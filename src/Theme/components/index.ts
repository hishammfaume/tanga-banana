import Buttons from "./buttons";
import Card from "./card";
import Stack from "./stack";
import TextField from "./text-field";

const components = Object.assign({}, Buttons, Card, Stack, TextField);

export default components;
